import { FormControl, styled, TextField } from '@mui/material';

export const FlexTextField = styled(TextField)`
    margin-inline: 1em;
    margin-top: 1em;
    margin-bottom: 1em;
    flex-grow: 1;
`;

export const FlexFormControl = styled(FormControl)`
    margin-inline: 1em;
    margin-top: 1em;
    margin-bottom: 1em;
`;
